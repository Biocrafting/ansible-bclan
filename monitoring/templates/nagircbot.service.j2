[Unit]
Description=Nagios IRC Bot
After=network.target

[Service]
User=nagios
ExecStart=/usr/local/bin/nagircbot -d -f /var/cache/icinga2/status.dat -s {{ icinga.irc_server }} -c '{{ icinga.irc_channel }}' -n {{ icinga.irc_nick }}
RestartSec=30
Restart=always

[Install]
WantedBy=multi-user.target
